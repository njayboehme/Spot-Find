# RSSI Collection for Heat Map or Multiantenna Direction Finding

Reports RSSI values for data packets received by the ESP32.

Build with `idf.py build`
To use run `idf.py flash monitor` from a terminal.
